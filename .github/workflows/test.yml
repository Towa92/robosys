name: test
on: on
jobs:
        test:
                runs-on: ubuntu-latest
                steps:
                        - uses: actions/checkout@v3
                        - name: All test
                          run: bash -xv ./test.bash


